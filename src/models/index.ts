import { createUserModel, loginUserModel } from "./userModel.validation";
import {
  createCommentsModel,
  updateCommetsModel,
} from "./commentsModel.validation";

export {
  createUserModel,
  loginUserModel,
  createCommentsModel,
  updateCommetsModel,
};
